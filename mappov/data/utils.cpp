#include "utils.h"


/* Print grid row by row. */
void print_xy_grid(float * grid, int xsize, int ysize, int xstride, int ystride)
{
    for (int x {0}; x < xsize*ysize*3; x += 3)
    {
        for (int y {0}; y < ysize; y++)
        {
            /*
            TODO 
            N = 3 * xsize * ysize */
        }
    }
}